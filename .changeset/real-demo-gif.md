---
'modernjs-typed-routes': patch
---

Docs: replace the placeholder demo GIF with the real recording (route autocomplete,
typo'd-path compile error, required params) — shown on the README, the npm page and
the docs home.
