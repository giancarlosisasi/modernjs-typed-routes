export default function LoginPage() {
  return (
    <div>
      <h1>login</h1>
      <p>
        Convention: pathless layout group (__auth/) — wraps /login in a layout
        without adding a URL segment.
      </p>
    </div>
  );
}
