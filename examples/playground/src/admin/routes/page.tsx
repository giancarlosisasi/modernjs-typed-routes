export default function AdminHomePage() {
  return (
    <div>
      <h1>admin-home</h1>
      <p>
        Convention: second entry (src/admin/routes) — served under the /admin
        basename with entry-scoped route types.
      </p>
    </div>
  );
}
